FROM python:3.13-slim

WORKDIR /app

# Install Python packages
COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

COPY VERSION .
COPY deploy/worker//health.py . 

RUN touch /tmp/healthy