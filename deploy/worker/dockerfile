FROM python:3.13-slim

WORKDIR /app

# Install Python packages
COPY requirements-sw.txt .
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements-sw.txt

COPY VERSION .
COPY . .

RUN touch /tmp/healthy